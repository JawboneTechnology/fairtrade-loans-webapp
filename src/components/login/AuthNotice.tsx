const AuthNotice = () => {
  return (
    <div className="text-center text-sm text-brand mt-4 rounded-md p-5 w-full bg-brand/10 border border-brand/30">
      <p className="text-base italic">
        Only members registered with JSTL Foundation are allowed to access this
        platform.
      </p>
    </div>
  );
};

export default AuthNotice;
