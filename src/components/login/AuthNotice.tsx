const AuthNotice = () => {
  return (
    <div className="text-center text-sm text-primary mt-4 rounded-md p-5 w-full bg-light">
      <p className="text-base italic">
        Only members of FairTrade Foundation are allowed to access this
        platform.
      </p>
    </div>
  );
};

export default AuthNotice;
